apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: skillpulse-staging
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/malathi-shetty/github-actions-kubernetes-masterclass.git
    targetRevision: main
    path: k8s/overlays/staging

  destination:
    server: https://kubernetes.default.svc
    namespace: skillpulse-staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
